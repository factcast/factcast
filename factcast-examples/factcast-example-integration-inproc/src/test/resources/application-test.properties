spring.datasource.url=jdbc:postgresql://${embedded.postgresql.host}:${embedded.postgresql.port}/${embedded.postgresql.schema}
spring.liquibase.user=${embedded.postgresql.user}
spring.liquibase.password=${embedded.postgresql.password}
spring.datasource.username=factcast-application-user
spring.datasource.password=factcast-application-user










  