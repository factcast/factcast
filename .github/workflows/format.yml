# This file was generated using Kotlin DSL (.github/workflows/format.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Format

on:
  push:

jobs:
  "check_yaml_consistency":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        name: Check out
        uses: actions/checkout@v3
      - id: step-1
        name: Execute script
        run: rm '.github/workflows/format.yml' && '.github/workflows/format.main.kts'
      - id: step-2
        name: Consistency check
        run: git diff --exit-code '.github/workflows/format.yml'
  "formatting":
    runs-on: "ubuntu-latest"
    needs:
      - "check_yaml_consistency"
    steps:
      - id: step-0
        name: CheckoutV3
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.PAT }}
      - id: step-1
        name: SetupJavaV3
        uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: corretto
      - id: step-2
        name: Execute Spotless
        run: ./mvnw -B spotless:apply --file pom.xml
      - id: step-3
        name: GitAutoCommitActionV4
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Apply formatter
