= Setup
:icons: font



This section will walk you through the many options of how to setup and operate FactCast-Server-UI.

First, decide for a link:deployment-options[deployment-option] and configure the link:properties[necessary Vaadin property].

Also there are several options regarding link:security[Security].

If you want to persist reports generated by users somewhere else than on the instance's filesystem take a look at
link:report-storage[Report Storage].