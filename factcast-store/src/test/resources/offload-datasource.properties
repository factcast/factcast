# using the same as primay here
factcast.store.offload.url=${spring.datasource.url}
# but with a readonly user
factcast.store.offload.username=${spring.datasource.username:}
factcast.store.offload.password=${spring.datasource.password:}
