#factcast.store.offload.driver-class-name=${spring.datasource.driver-class-name}
# using the same as primay here
factcast.store.offload.url=${spring.datasource.url}
factcast.store.offload.username=${spring.datasource.username:}
factcast.store.offload.password=${spring.datasource.password:}
#factcast.store.offload.type=org.apache.tomcat.jdbc.pool.DataSource
#factcast.store.offload.connection-properties=socketTimeout=0;preparedStatementCacheSize=0;
# this is a hint at best
#factcast.store.offload.default-read-only=true
